Execution commands / instructions:

a) "ant run" : executes target section of the build.xml file that both compiles and runs Java program
b) "ant clean" :  executes target section of the build.xml file that removes build folder and thus the subsequent .class and .jar files
c) "ant main" : executes clean and run target sections of the build.xml file
